/**
 * SEO Metadata for Diabetes Mouse Models
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Diabetes Mouse Models | ITL',
  description: 'Diabetes mouse models for studying insulin signaling, beta cell function, and glucose metabolism. Type 1 and Type 2 disease mechanisms.',
  path: '/diabetes-mouse-models',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Diabetes Mouse Models', path: '/diabetes-mouse-models' },
  ],
});
