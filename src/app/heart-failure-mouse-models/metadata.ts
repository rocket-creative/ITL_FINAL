/**
 * SEO Metadata for Heart Failure Mouse Models
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Heart Failure Mouse Models | ITL',
  description: 'Heart failure mouse models for studying cardiac remodeling, contractile dysfunction, and myocardial stress responses. Therapeutic testing platforms.',
  path: '/heart-failure-mouse-models',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Heart Failure Mouse Models', path: '/heart-failure-mouse-models' },
  ],
});
