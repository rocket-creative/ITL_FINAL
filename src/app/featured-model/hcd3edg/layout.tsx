/**
 * hCD3EDG Featured Model Layout
 * Exports metadata for SEO
 */

export { metadata } from './metadata';

export default function HCD3EDGLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
