/**
 * SEO Metadata for Tissue Specific Knockout Models
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Tissue Specific Knockout Models',
  description: 'Tissue specific knockout mice using Cre LoxP to delete genes in targeted cell types. Study gene function without embryonic lethality. Since 1998.',
  path: '/tissue-specific-knockout',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Custom Models', path: '/custom-mouse-models' },
    { name: 'Tissue Specific Knockout Models', path: '/tissue-specific-knockout' },
  ],
});
