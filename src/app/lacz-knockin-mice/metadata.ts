
import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'LacZ Knockin Mice',
  description: 'LacZ knockin mouse models for sensitive gene expression detection. Beta galactosidase enzymatic reporter with X-gal staining. Model generation since 1998.',
  path: '/lacz-knockin-mice',
});

