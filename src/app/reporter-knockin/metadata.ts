/**
 * SEO Metadata for Reporter Knockin Models
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Reporter Knockin Mouse Models',
  description: 'Reporter knockin mice for tracking gene expression and cell lineage. Insert GFP, lacZ, luciferase, or Cre at endogenous loci. Since 1998.',
  path: '/reporter-knockin',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Model Generation', path: '/custom-mouse-models' },
    { name: 'Reporter Knockin Models', path: '/reporter-knockin' },
  ],
});
