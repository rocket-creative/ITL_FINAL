/**
 * SEO Metadata for Critical Exon Selection
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Critical Exon Selection for Knockouts',
  description: 'Critical exon selection for effective gene knockouts. Target exons essential for protein function to ensure complete loss of gene activity.',
  path: '/critical-exon-selection',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Technologies', path: '/technologies' },
    { name: 'Critical Exon Selection', path: '/critical-exon-selection' },
  ],
});
