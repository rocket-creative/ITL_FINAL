/**
 * SEO Metadata for Start Your Project
 * Auto-generated for canonical URLs and structured data
 */

import { generateMetadata, generateBreadcrumbs } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Start Your Mouse Model Project',
  description: 'Request a consultation for your custom mouse model. Our scientists will help design the optimal strategy for your research goals.',
  path: '/start-your-project',
});

// BreadcrumbList structured data
export const breadcrumbSchema = generateBreadcrumbs({
  items: [
    { name: 'Home', path: '/' },
    { name: 'Start Your Project', path: '/start-your-project' },
  ],
});
