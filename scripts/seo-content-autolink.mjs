#!/usr/bin/env node
/**
 * One time helper: scan src/content and src/lib/search/siteIndex.ts for gene tokens,
 * then insert first match links to Tier 1/2/3 URLs. Review diffs manually before commit.
 * Not run in CI.
 */
console.log('seo-content-autolink: implement MDX and HTML parsing before bulk replace.');
process.exit(0);
