.ai-errors.log
.DS_Store

# Environment variables (NEVER commit these!)
.env.local
.env*.local

# Google service account credentials (NEVER commit!)
scripts/google-service-account.json
google-service-account.json
*-service-account.json

# Archive folder for old/unnecessary files
archive/

# Change documents (source files, not needed in repo)
changes/
Services-*.zip
Services-Corrections/

# Extracted content (generated from source docs)
itl-website/extracted-changes/

# Next.js
.next
.vercel
node_modules
tsconfig.tsbuildinfo

# Next 16: use src/proxy.ts only; a middleware.ts re-export conflicts with proxy.
/src/middleware.ts
